# Can't use the default, Chrome, since it just backgrounds itself, and then mutt
# goes and deletes the file Chrome was trying to read.
text/html; /usr/bin/w3m -T text/html '%s'; needsterminal; description=HTML Text; nametemplate=%s.html
